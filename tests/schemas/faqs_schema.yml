type: "//rec"
required:
  it:
    type: "//arr"
    length: { min: 1 }
    contents:
      type: "//rec"
      required:
        themes: "//str"
        question: "//str"
        answer: "//str"
      optional:
        id: "//str"
optional:
  en:
    type: "//arr"
    length: { min: 1 }
    contents:
      type: "//rec"
      required:
        themes: "//str"
        question: "//str"
        answer: "//str"
      optional:
        id: "//str"
