type: "//rec"
required:
  public:
    type: "//arr"
    length: { min: 1 }
    contents: "tag:widget"
  pa:
    type: "//arr"
    length: { min: 1 }
    contents: "tag:widget"
  allowed_widgets_preview:
    type: "//map"
    values:
      type: "//rec"
      required:
        params:
          type: "//any"
      optional:
        height: "//int"
